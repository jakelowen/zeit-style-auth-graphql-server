## create a postgres database for local dev / testing

`createdb ...`
and update the credentials in `src/knexfile.js`

## migrate database

`yarn migrateDb`

## test

`yarn test`

## run
`yarn`
`yarn start`
